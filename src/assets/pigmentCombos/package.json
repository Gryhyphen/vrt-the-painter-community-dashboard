{
  "name": "pigment-combo-pipeline",
  "version": "1.0.0",
  "description": "Pigment combo data pipeline automation",
  "type": "module",
  "scripts": {
    "run-extracts": "node --experimental-strip-types scripts/run-extracts.ts",
    "run-cleans": "node --experimental-strip-types scripts/run-cleans.ts",
    "run-enrich": "node --experimental-strip-types scripts/run-enrich.ts",
    "run-all": "npm run run-extracts && npm run run-cleans && npm run run-enrich"
  },
  "devDependencies": {
    "@types/node": "^25.6.0"
  }
}
